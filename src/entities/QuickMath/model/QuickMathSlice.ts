import { createSlice, PayloadAction } from "@reduxjs/toolkit"


interface initialStateType {

}

const initialState = {

} as initialStateType

const QuickMathSlice = createSlice({
  name: "QuickMathSlice",
  initialState,
  reducers: {

  }
})
export const { } = QuickMathSlice.actions
export const QuickMathReducer = QuickMathSlice.reducer
